Di routes/web.php, di dalam group role:guru, ADA DUA baris yang sama:

    Route::get('/konseling/{id}', [KonselingController::class, 'show'])->name('konseling.show');

HAPUS salah satu (biarkan hanya 1).

Biasanya urutannya seperti ini — HAPUS baris yang ditandai:

    Route::get('/konseling/{id}', [KonselingController::class, 'show'])->name('konseling.show');
    Route::post('/konseling/{id}/konfirmasi', ...);
    Route::post('/konseling/{id}/laporan', ...);
    Route::post('/konseling/{id}/batal', ...);
    Route::get('/konseling/{id}', [KonselingController::class, 'show'])->name('konseling.show');  // ← HAPUS BARIS INI

Jangan hapus yang di group siswa atau kepsek.
